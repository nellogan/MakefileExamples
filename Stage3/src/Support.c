#include "Support.h"

int Support(int support_number)
{
    int plus = 12;
    int sum = support_number + plus;
//    printf("Support called with support_number: %d, sum %d\n", support_number, sum);
    return sum;
}